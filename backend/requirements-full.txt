# 资产先锋后端完整依赖（核心 + 补充，一键恢复环境用）
# 由 requirements.txt + 临时安装但必需的包合并而来（2026-08-25）
# 安装：pip install -r requirements-full.txt

# --- 核心运行依赖 ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.20
openpyxl==3.1.5
httpx==0.28.1
jinja2==3.1.5
python-docx==1.1.2
pypdf==5.1.0
pytest==9.1.1

# --- 补充依赖（PDF 生成 / OCR / 调试，删除会坏功能）---
# PDF 报告生成（替代 WeasyPrint，纯 Python 无 GTK 依赖）
reportlab==5.0.1
pillow==12.3.0
# PDF 渲染预览 / 扫描 PDF OCR 前置
pymupdf==1.28.2
# 图片 OCR（土地价格库图片导入 / 扫描 PDF / 判决书图片）
rapidocr-onnxruntime==1.4.4
# 调试（CDP 浏览器定位 UI 问题）
websocket-client==1.9.0

# --- 已弃用（保留无害，勿依赖）---
# weasyprint==63.1  (Windows 缺 GTK 无法运行，PDF 已改用 reportlab)
